name: Validate

on:
  workflow_dispatch:

jobs:
  validate:
    uses: jgutierrezdtt/security-tutorial-base/.github/workflows/reusable-validate.yml@main
    with:
      tutorial-name: Secure Coding Mobile and IaC
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
